# 智能追剧

智能追剧负责跟踪连载剧、补齐缺失集和处理完结洗版。

## 展示规则

- “追剧中”只显示追剧、暂停、待定状态的**季度卡片**，标题格式为“剧名 第 X 季”，并显示“在追 X 季”。
- 季状态按本地集数收集进度判断，剧状态对齐 TMDb。
- “完结”列表才按整剧聚合展示。

## 刷新行为

刷新只提交新增集，以及元数据或剧照发生变化的集，不会全量重新注入整部剧。缺少剧照且开启截图时，会把截图任务接入同一任务链。

## 完结洗版

完结一致性校验未通过时，系统会为本季分集设置待洗版标志并提交完结洗版。只有整季一致性校验通过后，才会移除待洗版标志。
